import { test } from '@playwright/test';

/**
 * 06-me-tab — TODO skeleton.
 *
 * Coverage gap: Me tab settings (theme, notifications, account
 * deletion request, subscription status). Backend contract is
 * covered by MeControllerTest + AccountDeletionTest +
 * FrontendContractAuthTest (401 surface).
 *
 * Picked up once the design freeze on Me tab settings shape lands.
 */
test.skip('TODO: open Me tab → see subscription tier + journey stats', () => { /* no-op */ });
test.skip('TODO: toggle a setting → reload → setting persisted', () => { /* no-op */ });
test.skip('TODO: account delete-request flow → confirm → soft-delete confirmed', () => { /* no-op */ });
