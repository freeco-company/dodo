import { test } from '@playwright/test';

/**
 * 04-island — TODO skeleton.
 *
 * Coverage gap: island scene browsing + store entry + consume-visit.
 * Backend contract is covered in AlignmentEndpointsTest (`/island/*`).
 * UI flow involves the island canvas + scene store modals which
 * require deterministic entitlement seeds to be useful.
 *
 * Picked up in a follow-up ticket.
 */
test.skip('TODO: open island tab → see ≥1 scene → enter store → see items', () => { /* no-op */ });
test.skip('TODO: consume visit → assert visits_remaining decreased', () => { /* no-op */ });
